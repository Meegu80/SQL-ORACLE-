begin
    for i in 1..100 loop
    insert into board(bno,title,content, member_id, reg_date)
    values(
    i,
    '제목' || i,
    '내용' || i,
    'java',
    sysdate - trunc(dbms-random.value(0,365)),
    i
    );
    end loop;
    end;
    /

      -- 대량 더미 데이터 만들기 --
